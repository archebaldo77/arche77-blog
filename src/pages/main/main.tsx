// helpers;
import { classNames } from '../../helpers/class-names';

import classes from './main.module.scss';

const MainPage = () => <h2 className={classNames(classes.main)}>Main page</h2>;

export default MainPage;
